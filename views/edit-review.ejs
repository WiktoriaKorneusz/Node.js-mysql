<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include("includes/head.ejs", {title: "add review"}) %>
    </head>
    <body class="blue-grey darken-4 blue-grey-text text-lighten-5">
        <%- include("includes/header.ejs") %>
        <main class="container s12">
            <h1 class="center"><%= review.title %></h1>
            <form class="col s12" action="/update/<%= review.id %>" method="POST">
                
                
                <div class="row">
                    <div class="input-field col s12">
                        <textarea
                            id="summary"
                            class="materialize-textarea"
                            maxlength="250"
                            name="summary"
                            
                            required
                        ><%= review.summary %></textarea>
                        <label for="summary">Review summary</label>
                    </div>
                </div>
                <div class="row">
                    <div class="input-field col s12">
                        <textarea
                            id="body"
                            class="materialize-textarea"
                            name="body"
                            
                            required
                        ><%= review.body %></textarea>
                        <label for="body">Review content</label>
                    </div>
                </div>
                
                <div class="row">
                    <button
                        type="submit"
                        id="submit"
                        class="waves-effect waves-light btn cyan right btn-large"
                    >
                        Update review
                    </button>
                </div>
            </form>
        </main>
    </body>
</html>
